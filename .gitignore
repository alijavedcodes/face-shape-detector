__pycache__/
*.pyc
# Ignore model dir contents, but bundle the one model the app actually uses
# (via Git LFS) so the Space ships with it instead of downloading at runtime.
models/*
!models/shape_predictor_81_face_landmarks.dat
annotated_output.jpg
.venv/
.DS_Store

# annotated outputs
sample_images/annotated_*.jpg

# local result previews
results/
